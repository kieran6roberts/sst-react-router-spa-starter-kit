import type { Config } from "@react-router/dev/config";

export default {
  /* 
  This kit employs a SPA approach, you can change this to `true` to enable SSR, or opt employ
  static re-rendering. using the 'prerender' option.
  */
  ssr: false,
} satisfies Config;
